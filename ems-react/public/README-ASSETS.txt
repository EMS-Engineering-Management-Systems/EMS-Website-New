WHERE TO PUT IMAGES (pick one project — or both):

1) React site (npm run dev / npm run build in ems-react/)
   Put files HERE in this folder: ems-react/public/
   They load as /EMS.png, /SIEMENS.PNG, etc.

2) Legacy static page (index.html next to the ems-react folder)
   Copy the SAME filenames into the repo root (same folder as index.html).

Files this site expects:
- EMS.png
- SIEMENS.PNG, cisco.PNG, awsslogo.PNG, oraclelogo.PNG
- Optional video: Trial.mp4 at public root, or public/videos/trial.mp4 if you use that path in HTML.
